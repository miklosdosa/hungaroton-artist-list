const PARAM_CONF = {
  artists: {
    available: ["page", "per_page", "include_image", "search"],
    default: {
      include_image: "true",
      per_page: "50",
    },
  },
};

export { PARAM_CONF };
